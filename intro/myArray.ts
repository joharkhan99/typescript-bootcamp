const superHeros: string[] = [];
// const powers: number[] = [1, 2];
const powers: Array<number> = [];

type User = {
  name: string
  isActive: boolean
}

// array of user data types
const allUsers: User[] = [];

// array of arrays
const MLModels: number[][] = [
  [255, 25, 255],
]

// type any
const sample: any[] = [];

sample.push(12);
sample.push("ds")
sample.push(false)

superHeros.push("Spiderman");
superHeros.push("Thor");

powers.push(10);

allUsers.push({ name: "ss", isActive: false });